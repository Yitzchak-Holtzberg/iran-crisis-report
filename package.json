{
  "name": "iran-crisis-report",
  "version": "1.0.0",
  "description": "Iran Crisis Report — interactive news dashboard",
  "scripts": {
    "build": "node build.js",
    "validate": "node build.js --warn-only 2>&1 | tail -20"
  },
  "private": true,
  "dependencies": {
    "nunjucks": "^3.2.4"
  }
}
